import 'package:flutter/material.dart';import '../../core/notifications/local_notifications.dart';class NotificationsPage extends StatelessWidget{const NotificationsPage({super.key});@override Widget build(BuildContext c)=>Scaffold(appBar: AppBar(title: const Text('اعلان‌ها')),body: Center(child: FilledButton(onPressed: () async{await LocalNotifications.init();await LocalNotifications.notify(1,'یادآوری نوبت','ساعت 15:00 — مشتری');}, child: const Text('اعلان محلی تست'))));}