import 'dart:io';import 'package:firebase_core/firebase_core.dart';import 'package:firebase_messaging/firebase_messaging.dart';class PushService{static bool _initialized=false;static Future<void> init({required bool enable}) async{if(!enable||_initialized)return;await Firebase.initializeApp();await FirebaseMessaging.instance.requestPermission();if(Platform.isAndroid){await FirebaseMessaging.instance.subscribeToTopic('all');}_initialized=true;}}
